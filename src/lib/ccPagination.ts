/** Default rows-per-page for cost-center lists (Dashboard, Planner). */
export const DEFAULT_CC_PAGE_SIZE = 25

/** Selectable page sizes; "All" is rendered separately by the pagination component. */
export const CC_PAGE_SIZE_OPTIONS = [10, 25, 50, 100] as const
