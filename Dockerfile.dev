# syntax=docker/dockerfile:1.7
# Dev image: runs `vite` with HMR. Mount the source via docker compose.
FROM node:22-alpine
WORKDIR /app

COPY package.json package-lock.json ./
RUN npm ci

COPY . .

EXPOSE 5003
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
